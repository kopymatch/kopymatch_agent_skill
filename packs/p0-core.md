# P0 Core Pack — KopyMatch

8 skill đặc thù cho hệ thống anti-scam tài chính KopyMatch.

## Skills

1. `kopy-spec-sdd` — Đặc tả thiết kế hệ thống
2. `kopy-evidence-pack-schema` — Schema Evidence Pack JSON
3. `kopy-crawl-url-to-markdown` — Crawl URL → Markdown
4. `kopy-normalize-entity` — Chuẩn hoá entity
5. `kopy-risk-signal-extractor` — Trích xuất tín hiệu rủi ro
6. `kopy-trace-report` — Báo cáo truy vết
7. `kopy-marketplace-chat-evidence` — Bằng chứng chat marketplace
8. `kopy-finetune-dataset-kit` — Dataset kit fine-tune

## Install

```bash
bash scripts/install-both.sh /path/to/project
```
